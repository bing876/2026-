# Capability Registry

## Principle

Models are described by capability scores, not fixed roles.

Example:

```
reasoning
coding
writing
research
vision
planning
review
long_context
```

The registry allows Router to dynamically select execution resources.
