# Execution Schema

Every runtime execution should contain:

- execution_id
- user_id
- task_id
- primary_model
- selected_models
- capabilities
- budget_limit
- status
- result
- telemetry_reference

The schema is the shared contract between runtime modules.