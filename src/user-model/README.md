# User Model System

Manages platform models and user-provided API models.

Goals:
- User can select a primary model.
- Platform models can be used through controlled access.
- User API models can participate in orchestration.
- Credentials remain isolated from agents.
